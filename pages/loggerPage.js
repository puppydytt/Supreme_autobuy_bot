export const LoggerPage = () => {
    const newElement = document.createElement('a');
    newElement.text = 'Coming soon: Logger';
    newElement.className += 'loggerPage';

    return newElement;
};

